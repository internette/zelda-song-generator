{
  "name": "zelda-song-generator",
  "version": "1.0.0",
  "scripts": {
    // "postinstall": "npm install --prefix db/server && npm install --prefix app"
    "start": "node db/server/zelda-server.js",
    "heroku-postbuild": "cd app && npm install --only=dev && npm install && npm run build"
  },
  "author": "",
  "license": "ISC"
}
